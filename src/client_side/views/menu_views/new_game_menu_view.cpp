#include "new_game_menu_view.h"


NewGameMenuView::NewGameMenuView() : AbstractMenuView(menu_name_) {
  // scores = model.LoadScores();
  // std::vector<std::shared_ptr<AbstractView>> v = {}
  // for (score : scores) {
  //     v.push_back(std::make_shared<Label>(this, score.text));
  // }
}